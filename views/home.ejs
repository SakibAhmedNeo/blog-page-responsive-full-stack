<%- include("partials/header.ejs"); -%>

<h1>Home</h1>

<p> <%= startingContent %> </p>

<% posts.forEach(function(post) { %>
<h1> <%= post.title %> </h1>
<p> <%= post.content.substring(0, 10) %>
<%  if (post.content.length > 10) {%>
    ...
    <a href="post/<%= post._id %>">Read More</a>
<%  };%>

</p>
<% }); %>

<%- include("partials/footer.ejs"); -%>
